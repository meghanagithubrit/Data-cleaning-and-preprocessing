# Data-cleaning-and-preprocessing
Import dataset and check missing values. Fill missing values (median for numbers, mode for categories). Convert categorical data into numbers (encoding). Normalize numerical features. Detect and remove outliers using boxplots and IQR method. Save the cleaned dataset for ML use.
